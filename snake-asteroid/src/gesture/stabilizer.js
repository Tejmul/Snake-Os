/**
 * GestureStabilizer — debounce, frame voting, and confidence filtering.
 *
 * Uses a 7-frame sliding window with 4/7 agreement threshold and
 * 250ms debounce to prevent jitter from noisy thumb gestures.
 */

// Placeholder — implementation in commit 4
export class GestureStabilizer {
  constructor() {}
  push(_gesture) { return null; }
  reset() {}
}
